#include "somejuice.cpp"
//i NEED DO THIS SHIT BECAUSE  fucking mock , it's relyy not need there
class Jucii{
    StreamProcessor* processor;
    public:
        Jucii(StreamProcessor* _processor) : processor(_processor) {}
        void juc_juc(std::string param){
            if (param == "J_Juciii") processor -> Process(Operation::decoding);
            else {
                //HAHAHAHA
            }
        }
};